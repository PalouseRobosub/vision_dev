Distributions are hosted on PyPI. Upload is completed using twine:
    `pip install twine`

To generate a distribution:
    1) Update the version number in setup.py
    2) Run `python setup.py sdist`
    3) Run `twine upload dist/rslabel-{VERSION}.tar.gz
    4) Username is PalouseRobosub. Password can be acquired from leadership.
